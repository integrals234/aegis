"""AEGIS M4 research: calendar-spread construction, features, hedge-ratio
estimation and the leakage-free signal reference (AEGIS-076..081).

`python-research` may depend on `python-common`, `python-data` and
`python-futures` only (`configs/architecture_rules.yaml`) -- every price,
contract and roll concern here is built on the M2 futures stack, never
duplicated.
"""
