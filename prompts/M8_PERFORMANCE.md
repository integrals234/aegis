# M8 Prompt — Performance Engineering

Only after equivalence tests pass, add preallocated pools, arenas/free lists, intrusive queues, bounded lock-free MPSC/output queues, single-writer sharding, optional affinity and cache/layout optimizations. Benchmark baseline vs optimized with full methodology and tail statistics. Never invent targets.
