# M0 Prompt — Foundation

Build only the governance and engineering foundation. Establish C++/Python toolchains, test layers, CI, config, structured logging, documentation, dependency checks, experiment manifest schema and sample-data policy. Do not implement an exchange, strategy, risk engine, dashboard or performance optimization.

Use `/plan-milestone M0`, obtain approval, then `/implement-milestone M0`. Finish with `/audit-milestone M0`; remediate all failures before `/close-milestone M0`.
