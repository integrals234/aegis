{
  "allowlist_version": 1,
  "policy": "Every entry needs a reason. The allowlist exists for files whose purpose is to contain secret-shaped text; it is never the place to silence a real finding.",
  "allow": [
    {
      "path": "tools/scan_secrets.py",
      "reason": "The scanner's own rule table names credential keywords; it holds no credential values."
    },
    {
      "path": "tests/unit/test_scan_secrets.py",
      "reason": "Negative fixtures assert the scanner fires; values are documented non-live examples."
    },
    {
      "path": "tests/unit/fixtures/secrets_bad/*",
      "reason": "Deliberately failing fixture tree, scanned explicitly by the negative-gate test."
    },
    {
      "path": "configs/secret_scan_allowlist.json",
      "reason": "This file names the allowlisted paths and contains no credential values."
    }
  ]
}
