{
  "config_version": 1,
  "run": {
    "experiment_id": "m0-full-2026-08-06",
    "seed": 424242,
    "description": "Every field the v1 schema allows, so the corpus exercises them all."
  },
  "logging": { "level": "debug", "format": "jsonl", "destination": "experiments/logs/run.jsonl" },
  "paths": { "output_dir": "experiments/out", "data_dir": "data_samples" },
  "metrics": { "enabled": true, "snapshot_interval_ms": 250 }
}
