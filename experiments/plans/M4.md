# AEGIS M4 — First Rigorous Calendar-Spread Strategy: Plan of Record

> **Status:** owner-approved plan of record. Activated by PR #10.
> **Approved:** 2026-08-17, under explicit time/quota constraints: build-first
> cadence, one tiny activation PR, two build batches, no intermediate
> checkpoint, one closure.

**Context.** M0–M3 are closed; canonical main is `f149aac` (PR #9). The
platform has a full participant pipeline (feed → book → statistics → OMS →
portfolio) and zero strategy code. `cpp/participant/strategy`,
`python/research` and `python/reports` are all dated M4 and empty — the
reason AEGIS-004 and AEGIS-024 have carried open residuals since M0/M2. M4
builds the first real strategy: a calendar spread, trading through the real
M1–M3 infrastructure.

---

## 1. STARTING STATE

| Fact | Value |
|---|---|
| Canonical main | `f149aacaa82a3a40294fd674b208740198d07232` (PR #9) |
| Catalogue before M4 | 85 verified, 12 implemented, 141 not_started |
| M4 requirements | 6 — AEGIS-076..081, all `must`, all `not_started` |
| Obligations due at M4 | 2 — AEGIS-004 (M0), AEGIS-024 (M2) |
| Strategy/research/reports code on disk | zero lines |

## 2. EXACT M4 REQUIREMENT SET

| ID | Title | Frozen acceptance |
|---|---|---|
| AEGIS-076 | Calendar spread construction | Research dataset can reproduce every spread observation |
| AEGIS-077 | Term structure features | Feature tests use known curve shapes |
| AEGIS-078 | Calendar-spread hedge ratio | Synthetic and historical tests validate estimates |
| AEGIS-079 | Calendar-spread stationarity | Report includes chosen test, assumptions, caveats |
| AEGIS-080 | Calendar-spread z-score signals | Signal timestamps use only contemporaneously available data |
| AEGIS-081 | Expiry and roll effects | Attribution report includes roll/expiry slices |

## 3. INHERITED OBLIGATIONS

- **AEGIS-004** (Exchange/participant separation): declared and unexercised
  since `cpp/participant/strategy` was empty. Paid in Batch 1 in full — real
  strategy code populates the layer, and the same strategy is exercised
  against real M1 matching (`tests/cpp/unit/
  test_calendar_spread_exchange_integration.cpp`).
- **AEGIS-024** (Roll-method sensitivity): M2 delivered the roll-date/
  adjusted-price-path comparison; the "strategy differences" half needs a
  strategy. Paid in Batch 2, once the strategy exists to compare across roll
  policies.

## 4. ARCHITECTURE

One approved delta to `configs/architecture_rules.yaml`
(`m4-architecture-transition`, PR #10): `cpp-participant-app.may_depend_on +=
cpp-participant-strategy`. `cpp-participant-strategy`'s own `may_depend_on`
(`[cpp-common, cpp-events, cpp-participant-book-builder, cpp-statistics]`)
was already declared at M0 and needed no change. See ADR-0025 for the full
strategy/OMS/exchange boundary and ADR-0026 for the leakage-free signal
convention every estimator follows.

| Module | Depends on | Responsibility |
|---|---|---|
| `cpp/participant/strategy` | common, events, book-builder, statistics | `CalendarSpreadStrategy`: proposals only |
| `python/research` | common, data, futures | Spread construction, features, hedge ratio, signal reference, stream builder |
| `python/reports` | common, data, futures, research | Shared deterministic report foundation (Batch 1); report content (Batch 2) |

## 5. FIRST REAL-DEAL DEMO

`aegis_participant_run --calendar-spread --stream
tests/unit/fixtures/participant/calendar_spread_stream.jsonl` — production
composition, deterministic, no exchange dependency. Real-matching proof:
`test_calendar_spread_exchange_integration.cpp`, the same strategy against a
real M1 `ExchangeNode`. See ADR-0025 for the disclosed construction of the
demo's two-leg quote stream.

## 6. BUILD BATCHES

**Batch 1** — strategy core, real M1 matching, first real-deal demo, report
foundation, AEGIS-076/077/078/080, AEGIS-004 in full.

**Batch 2** — stationarity (AEGIS-079), roll/expiry attribution (AEGIS-081),
AEGIS-024's strategy-comparison closure, report content, Python signal
reference cross-check.

No intermediate checkpoint. Straight from Batch 2 to closure.

## 7. TESTING CADENCE

Ordinary batches: focused tests for new functionality, build touched
targets, targeted format/static checks. No full suites, no Release/ASAN,
no evidence audit, no independent audit until closure.

## 8. OWNER ACTIONS

Two: approve/merge the M4 activation PR (#10, merged); approve/merge the
final M4 closure PR.

## 9. VERDICT

READY FOR M4 BATCH 1 — executing.
